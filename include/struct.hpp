#ifndef __MOTOR_STRUCT_HPP__
    #define __MOTOR_STRUCT_HPP__

// @brief structure to allow point of 2 float coordinate.
typedef struct {
    float x;
    float y;
} mt_vector2f_t;

// @brief structure to allow point of 2 int coordinate.
typedef struct {
    int x;
    int y;
} mt_vector2i_t;

#endif
